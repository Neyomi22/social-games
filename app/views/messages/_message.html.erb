<div class="message-container" id="message-<%= message.id %>">
  <i class="author">
    <span><%= message.user.email %></span>
    <small><%= message.created_at.in_time_zone("Melbourne").strftime("%a %b %e at %l:%M%p") %></small>
  </i>
  <p><%= message.content %></p>
</div>