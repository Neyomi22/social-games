
<%= simple_form_for @events do |f| %>
	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :lacation %>
	<%= f.input :sport, as: :select %>
	<%= f.input :number_of_participant %>
	<%= f.input :date %>
	<%= f.button	:submit %>
<% end %>